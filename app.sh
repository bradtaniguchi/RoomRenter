#!/usr/bin/env bash
# This script is used to start the program on linux machines
# use the alternate app.exe script to run the program on windows machines.
# no one has a mac, so there isn't a run file for it, sorry!